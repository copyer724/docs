# nest-cli

## 安装

方式一：

```bash
npx @nestjs/cli new 项目名
```

方式二（推荐）：

```bash
npm i -g @nestjs/cli
$ nest new project-name
```

当然方式二，存在缺点，就是 nestjs/cli 更新了，需要手动更新

```bash
npm update -g @nestjs/cli
```

## 辅助命令

```bash
nest -h
```

组合命令：`nest <commands> [options]`
